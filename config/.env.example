# This app needs NO secrets — it runs entirely in the browser.
# This file is kept only to match the Krowne template structure.
# If a backend is ever added, list required key NAMES here with fake values,
# and put the real values in config/.env (which is gitignored, never committed).
